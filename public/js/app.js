(($) => {
  /*let createTaskForm = document.querySelector("#createTaskModal form");
  createTaskForm.addEventListener("submit", async (e) => {
    e.preventDefault();
    console.log();
    let formData = new FormData(e.target);
    let response = await fetch("/", {
      method: "POST",
      body: formData,
    });
    console.log(response);
  });*/
})(document);
